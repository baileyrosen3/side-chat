[package]
name = "peek-parakeet"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
parakeet-rs = { version = "=0.3.7", default-features = false, features = ["cpu", "load-dynamic", "api-28"] }
serde_json = "1.0"
